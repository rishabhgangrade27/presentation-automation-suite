# top-level env / local files
.env
.env.local
*.local

# node
node_modules/
npm-debug.log*

# build outputs
dist/
.next/
out/

# local n8n runtime data (SQLite DB + encryptionKey config, generated by
# n8n/run_native.sh -- never commit this, it's a live secret + full workflow DB)
n8n/.n8n_native_data/

# misc
.DS_Store
*.log
